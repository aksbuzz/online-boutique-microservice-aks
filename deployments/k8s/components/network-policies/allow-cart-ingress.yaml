apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-cart-ingress
spec:
  podSelector:
    matchLabels:
      app: cart-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: checkout-service
        - podSelector:
            matchLabels:
              app: frontend
        - podSelector:
            matchLabels:
              app: envoy
      ports:
        - port: 5001